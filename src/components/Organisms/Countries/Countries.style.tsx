import styled from "styled-components";

export const Countries = styled.div`
  flex: 1;
  display: flex;
  flex-wrap: wrap;
  justify-content: space-evenly;

  overflow-y: scroll;
  overflow-x: hidden;
  gap: 10px;
`;
